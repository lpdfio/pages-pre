<lpdf version="1"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:noNamespaceSchemaLocation="../../schema/lpdf.xsd">
  <tokens>
    <colors>
      <color name="primary"  value="#1763cf" />
      <color name="surface"  value="#ffffff" />
      <color name="bg"       value="#f5f7fa" />
      <color name="accent"   value="#e8f0fc" />
      <color name="muted"    value="#737373" />
      <color name="rule"     value="#dde3ed" />
      <color name="danger"   value="#c0392b" />
    </colors>
  </tokens>
  <document size="a4" margin="40pt">
    <meta title="lpdf Showcase — Encryption" subject="RC4-128 · permission flags · owner password" />

      <section background="surface">
        <layout>
        <stack gap="xl">

          <!-- Header -->
          <flank align="center">
            <stack gap="xs">
              <text font-size="xxl" font="Helvetica-Bold" color="primary">Encryption</text>
              <text font-size="m" color="muted">RC4-128 · permission flags · open and owner passwords</text>
            </stack>
            <frame padding="s" background="danger" radius="2pt">
              <text font-size="s" font="Helvetica-Bold" color="#ffffff">CONFIDENTIAL</text>
            </frame>
          </flank>

          <divider color="rule" thickness="0.5pt" />

          <!-- How it works -->
          <stack gap="m">
            <text font-size="l" font="Helvetica-Bold" color="primary">How It Works</text>
            <text font-size="m">PDF encryption (RC4-128, algorithm V=2 R=3) is applied as a post-processing pass after document assembly. The engine derives a 128-bit file encryption key from the password and permission flags, then encrypts all string literals and stream bytes in-place. Object offsets stay valid because RC4 is length-preserving.</text>
          </stack>

          <!-- API -->
          <stack gap="m">
            <text font-size="l" font="Helvetica-Bold" color="primary">API</text>
            <text font-size="m" color="muted">Call setEncryption before renderPdf — the PDF bytes are then returned already encrypted.</text>
            <frame background="bg" padding="m" radius="4pt" border="0.5pt rule">
              <stack gap="xs">
                <text font-size="m" font="Helvetica-Bold">Permissions only (no open password)</text>
                <text font-size="m">engine.setEncryption("", "s3cr3t", { print: false, copy: false })</text>
                <text font-size="m" color="muted">File opens without prompting; cooperative viewers enforce the flags.</text>
              </stack>
            </frame>
            <frame background="bg" padding="m" radius="4pt" border="0.5pt rule">
              <stack gap="xs">
                <text font-size="m" font="Helvetica-Bold">With open password</text>
                <text font-size="m">engine.setEncryption("password", "owner", { copy: false })</text>
                <text font-size="m" color="muted">Viewers prompt for the user password before displaying content.</text>
              </stack>
            </frame>
          </stack>

          <!-- Permission flags table -->
          <stack gap="m">
            <text font-size="l" font="Helvetica-Bold" color="primary">Permission Flags</text>
            <table cols="1fr 2fr" border="0.5pt rule" stripe="bg">
              <thead background="accent">
                <td padding="s"><text font-size="m" font="Helvetica-Bold">Flag</text></td>
                <td padding="s"><text font-size="m" font="Helvetica-Bold">Effect when disabled</text></td>
              </thead>
              <tr>
                <td padding="s"><text font-size="m" font="Helvetica-Bold">print</text></td>
                <td padding="s"><text font-size="m">Printing disabled in viewer</text></td>
              </tr>
              <tr>
                <td padding="s"><text font-size="m" font="Helvetica-Bold">copy</text></td>
                <td padding="s"><text font-size="m">Text and graphic extraction blocked</text></td>
              </tr>
              <tr>
                <td padding="s"><text font-size="m" font="Helvetica-Bold">modify</text></td>
                <td padding="s"><text font-size="m">Content modification blocked</text></td>
              </tr>
              <tr>
                <td padding="s"><text font-size="m" font="Helvetica-Bold">annotate</text></td>
                <td padding="s"><text font-size="m">Adding or modifying annotations blocked</text></td>
              </tr>
              <tr>
                <td padding="s"><text font-size="m" font="Helvetica-Bold">fill_forms</text></td>
                <td padding="s"><text font-size="m">Form field filling disabled</text></td>
              </tr>
              <tr>
                <td padding="s"><text font-size="m" font="Helvetica-Bold">assemble</text></td>
                <td padding="s"><text font-size="m">Page insertion, deletion, rotation blocked</text></td>
              </tr>
              <tr>
                <td padding="s"><text font-size="m" font="Helvetica-Bold">print_hq</text></td>
                <td padding="s"><text font-size="m">High-quality printing degraded to low resolution</text></td>
              </tr>
            </table>
          </stack>

          <!-- Caveat -->
          <frame background="accent" padding="m" radius="4pt" border="0.5pt rule">
            <stack gap="xs">
              <text font-size="m" font="Helvetica-Bold" color="primary">Note on strength</text>
              <text font-size="m">Permission flags are advisory — cooperative viewers honour them, but tools such as qpdf can strip them in seconds when the open password is empty. They are a deterrent, not a lock. For strong access control, pair with an open password.</text>
            </stack>
          </frame>

        </stack>
        </layout>
      </section>
  </document>
</lpdf>
